package advance_java.annotations;
// Todo why we Annotations
/**
 * Annotation have number of uses:
 *      Information for the compiler-(Annotation can be used by the compiler to detect errors or surpass warnings)
 *      Compile Time & Deployment Time Processing- (Software Tools can process annotations information to generate code, XML files, & so forth)
 *      Runtime processing (Some annotation are available to be examined at runtime)
 */
//Todo Intro to Annotations

/**
 * Annotations are form of mata data, provides data about program, that is not
 * part of the program itself. Annotations have no direct effect on the operation
 * of the code they annotate.
 */

public class Annotations {
}
